
WORK IN PROGRESS:
----------------

# create virtual environment and install requirements:
cd dit-helpdesk
python3 -m venv venv/
source venv/bin/activate
pip install -r requirements.txt

export PYTHONPATH=$(pwd)

# to use 'dev' settings do:
export DJANGO_SETTINGS_MODULE=dit_helpdesk.settings.dev

# this uses the development sqlite database with prepolpulated data (located at: dit_helpdesk/dit_helpdesk_db.sqlite3)
# Be careful when committing changes on this to github.
# The Django superuser on this development DB is username: 'dit', password: 'dit'


# If you're working with a fresh database:

# 1. Run migrations
python manage.py migrate

# 2. add countries to DB
django-admin loaddata countries_data.json

# 3. create a superuser
python manage.py createsuperuser

